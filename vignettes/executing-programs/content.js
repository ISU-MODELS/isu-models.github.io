window.vignetteContent = "# Session 1. Executing programs\n\n**\u00a9 2026 Ryan P. McGehee, Ph.D.**\n\n## Introduction\n\nThis hour is how you make a computer run a job you can name: a Python file, a short shell script, or a tool like Grok Build, with the files and inputs you meant, in the folder you meant.\n\nIf **Launch Python Console** is active in the top bar, you can run the numbered checks on the lab host. If it is greyed out, copy the same commands into a terminal on your own machine. Either way, install Python locally. The console is a convenience when the lab host is running.\n\nA command line is a ticket: the program is the station, the extra words are notes on the ticket, and text you pipe in is the bag of ingredients. `python`, `grok`, and a `.bat` or `.sh` file are all programs the shell can start. You write a file, stand in a folder, type a line, and read what comes back, including the error.\n\nUseful words, in the sense we will use them:\n\n- **Shell:** reads the line you type (or a line from a script) and starts other programs.\n- **Terminal:** the window where you talk to the shell.\n- **Program:** a file the OS can start, such as `python.exe`, `grok.exe`, or `start.bat`.\n- **Interpreter:** Python itself; it reads `.py` files and carries out the instructions.\n- **PATH:** folders the shell searches when you type `python` or `grok` with no folder in front.\n- **Current directory:** the folder the shell is standing in; relative names start here.\n- **Argument:** extra text after the program name (`--name Ada`, a filename).\n- **Standard input:** text piped in, as if you were typing at the program.\n- **Virtual environment:** a private package set for one project.\n\n## Install Python and an editor\n\n### Download Python\n\nGet a current **stable** release from [python.org/downloads](https://python.org/downloads). Skip betas for this course; scientific libraries trail the newest interpreter.\n\nOn Windows, check **Add python.exe to PATH** in the installer. If you skip it, `python` will not work in a new terminal until you repair PATH.\n\nOn a Mac, Homebrew (`brew install python`) or the official installer both work. On Linux, use the distro package (`python3`).\n\nOpen a **new** terminal and run:\n\n```text\npython --version\n```\n\nOn some Mac and Linux setups the command is `python3`. Remember which one you have. If Launch Python Console is active, that is check **1**.\n\n### VS Code\n\nCode is text. Word will mangle quotes. Use [VS Code](https://code.visualstudio.com/) or another code editor, install the Python extension, and open a folder as the workspace. `Ctrl+Shift+P` (Windows) or `Cmd+Shift+P` (Mac), then \"Python: Select Interpreter\", and pick the Python you just installed.\n\nThe editor does not replace the shell. It makes the files easier to see. You still run them from a terminal.\n\n## Folders and paths\n\nEvery command runs from a current directory. Relative names (`lab1\\run.py`, `./run.py`) start there. Absolute names start from a drive or from `/` (`C:\\Users\\you\\lab1\\run.py`, `/Users/you/lab1/run.py`).\n\nPATH is where the shell looks for **programs**, not your scripts. `python` is found via PATH. `demoPrintScript.py` is found relative to the current directory unless you give a full path.\n\nIf a script is not executable on Mac or Linux (`Permission denied`), `chmod +x script.sh` is the usual fix. On Windows, look at Properties or `icacls`. In this course you will mostly call `python file.py`.\n\nGrok's `--cwd` is the same idea: it sets the folder the tool treats as home so relative paths match the project you meant.\n\n## Run a Python file\n\nA `.py` file is not a program by itself. Python is the program; the file is the job.\n\n```text\npython demoPrintScript.py\n```\n\nThat is check **2** in the console, or the same line in your own terminal. You should see `Hello, World!`\n\nIf you see `FileNotFoundError` or \"can't open file\", the shell is in the wrong folder, or the name is misspelled. Print the current directory (`cd` on Windows with no argument, `pwd` on Mac/Linux). Use Tab to complete names.\n\nOn your laptop, `cd` into the folder that holds the file, or pass a path. The lab console already stands in the lesson folder.\n\n## Command-line arguments\n\nAnything after the script name is an argument. Python exposes the whole line as `sys.argv`: item 0 is the script name, item 1 is the first extra word.\n\n```text\npython demoPrintArgScript.py Ada\n```\n\nCheck **3** runs that with `Ada`. You should see `Ada` in the printed list.\n\nTyping positions by hand gets old. `argparse` lets you name them:\n\n```text\npython demoArgParseScript.py -h\npython demoArgParseScript.py --name Ryan\n```\n\nCheck **4** is the named flag. Same idea as `grok --cwd ...`: a flag, then a value.\n\nIf a program prints help and quits, you probably passed `-h` or forgot a required flag. Read the help; it is the contract.\n\n## Pipes and standard input\n\nSome programs read a stream of text. That stream is standard input. You can type it, or pipe another command's output in:\n\n```text\necho Hello World | python demoStdinScript.py\n```\n\nOn PowerShell, `echo` still works for a short line. This script uppercases each non-empty line. Check **5**.\n\nUse arguments for short, named settings. Use a pipe or a redirected file when the data is longer.\n\n## Virtual environments\n\n`pip install` into the interpreter you use for everything will eventually break something. A virtual environment is a project-local Python: same language, its own package list.\n\n```text\npython -m venv .venv\n```\n\nActivate it before you install:\n\n```text\n.venv\\Scripts\\activate\n```\n\nOn Mac and Linux: `source .venv/bin/activate`.\n\nThen:\n\n```text\npip install numpy\npip install -r requirements.txt\n```\n\n`requirements.txt` in this lesson is a pinned list for later demos. On your own laptop, create the venv in the project folder, activate it, and install from the file. The prompt usually shows `(.venv)` when it is on.\n\nDo not commit a `venv` folder into a Google Drive tree. Keep environments outside shared vaults.\n\n## Shell scripts\n\nIf you can type a command, you can save it.\n\nOn Windows, `start.bat` in this lab changes to the lab folder and starts the server. On Mac and Linux you would write a `.sh` file and `chmod +x` it.\n\nA shell script can call Python, call `grok`, copy files, or chain pipes. It is still just lines the shell would have accepted one at a time. Put flags you always want (cwd, a log path) in the script so you do not retype them.\n\nWhen a script fails, the error is usually from the line it was running. Comment the flags (`rem` / `::` on Windows, `#` on Unix) so a future you can see why they are there.\n\n## Before you go\n\nYou should be able to find Python, stand in a folder, run a `.py` file, pass arguments, pipe text, keep packages in a venv, and treat `grok` and a `.bat`/`.sh` file as the same kind of object as `python`: a program plus a line.\n\nIf a later session \"does nothing,\" check three things in order: is the program on PATH, are you in the right folder, and did you pass the arguments that tool actually reads.\n";
