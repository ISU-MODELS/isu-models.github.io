window.vignetteContent = "# Session 1. Executing programs\n\n**\u00a9 2026 Ryan P. McGehee, Ph.D.**\n\n## The whole session, in one sentence\n\nThis session is about making a computer run a named job from a command line: a Python file, a small shell script, or a tool like Grok Build, with the files and inputs you meant, in the folder you meant.\n\nEverything else here is in service of that. If you can point at a file, say which program should read it, and pass it a few words or a stream of text, you can run the rest of this course, and you can run the tools we actually use in the lab.\n\nIf the host status above says the Python console is available, use the numbered buttons when the text says try it here. If the host is offline, copy the same commands into a terminal on your own computer. You should still install Python locally; the console is a convenience when the lab machine is running, not a replacement for that install.\n\n## A picture you can keep\n\nThink of a kitchen ticket. The ticket names the dish, maybe a few notes (\"no onions\", \"extra sauce\"), and it goes to a station that already knows the recipe. The ticket is the command. The station is the program. The notes are arguments. If the ticket is attached to a bag of ingredients shoved through the window, that bag is standard input.\n\nPython is one station. `grok` is another. A `.bat` or `.sh` file is a stack of tickets you wrote down so you do not retype them. None of those is magic. Each is a program the shell can find and start.\n\nThe slow, confusing way to learn this is to treat the editor, the language, and the terminal as three different subjects. They are one workflow: you write a file, you stand in a folder, you type a line, you read what comes back, including the error.\n\n## One-sentence dictionary\n\nRead this list first. The sections after it unpack the same words.\n\n- **Shell:** the program that reads the line you type (or a line from a script) and starts other programs.\n- **Terminal:** the window where you talk to the shell.\n- **Program:** a file the operating system can start, such as `python.exe`, `grok.exe`, or `start.bat`.\n- **Interpreter:** Python itself; it reads `.py` files and carries out the instructions.\n- **Script:** a text file of commands or Python statements you intend to run more than once.\n- **PATH:** the list of folders the shell searches when you type a name like `python` or `grok` with no folder in front.\n- **Current directory:** the folder the shell is standing in; relative names are measured from here.\n- **Argument:** extra text after the program name, like a filename or `--name Ada`.\n- **Standard input:** text piped into a program, as if you were typing at it, often from another command.\n- **Virtual environment:** a private copy of Python packages for one project, so versions do not collide.\n\n## 1. The shell, Python, and programs like Grok\n\nWhen you open a terminal, you are not \"in Python.\" You are in a shell. On this Windows lab that is usually Command Prompt or PowerShell. On a Mac it is zsh or bash. The prompt is waiting for a line.\n\nA line is: program, then optional arguments.\n\n```text\npython demoPrintScript.py\ngrok --cwd C:\\Cloud\\Brain\n```\n\n`python` and `grok` are both programs. The difference is what they do after they start. Python reads a `.py` file (or a snippet). Grok Build starts an agent session, often with `--cwd` so it knows which project folder to treat as home, and sometimes `--resume` plus an id if you are continuing a session.\n\nIf the shell says it cannot find `python` or `grok`, it looked through PATH and failed. That is a finding problem, not a Python-syntax problem. Fix PATH, or type the full path to the program, before you debug the script.\n\n**Try it here:** button **1. python --version**. You should see a version line. If the lab says unavailable, the server on this computer is not running.\n\n## 2. Install Python and a place to edit\n\n### Python\n\nGet a current **stable** release from [python.org/downloads](https://python.org/downloads). Skip betas for this course. Scientific libraries trail the newest interpreter.\n\nOn Windows, check **Add python.exe to PATH** in the installer. If you skip it, `python` will not work in a new terminal until you repair PATH.\n\nOn a Mac, Homebrew (`brew install python`) or the official installer both work. On Linux, use the distro package (`python3`).\n\nThen open a **new** terminal and run:\n\n```text\npython --version\n```\n\nOn some Mac and Linux setups the command is `python3`. Remember which one you have.\n\n### An editor\n\nCode is text. Word will mangle quotes and add encoding you do not want. Use [VS Code](https://code.visualstudio.com/) (or another code editor), install the Python extension, and open a folder as the workspace. `Ctrl+Shift+P` (Windows) or `Cmd+Shift+P` (Mac), then \"Python: Select Interpreter\", and pick the Python you just installed.\n\nYou can still run files from the terminal. The editor does not replace the shell; it makes the files easier to see.\n\n## 3. Where you are standing (paths)\n\nEvery command runs from a current directory. Relative names (`lab1\\run.py`, `./run.py`) start there. Absolute names start from a drive or from `/` (`C:\\Users\\you\\lab1\\run.py`, `/Users/you/lab1/run.py`).\n\nPATH is different: it is where the shell looks for **programs**, not your scripts. `python` is found via PATH. `demoPrintScript.py` is found relative to the current directory unless you give a full path.\n\nIf a script is not executable on Mac or Linux (`Permission denied`), `chmod +x script.sh` is the usual fix. On Windows, look at the file Properties or `icacls`. For this course you will mostly call `python file.py` and `python` already has permission.\n\nGrok's `--cwd` is the same idea: it sets the folder the tool treats as home, so later relative paths inside that session match the project you meant.\n\n## 4. Run a file\n\nA `.py` file is not a program by itself. Python is the program; the file is the job.\n\n```text\npython demoPrintScript.py\n```\n\n**Try it here:** button **2. Run a file**. You should get `Hello, World!`\n\nIf you see `FileNotFoundError` or \"can't open file\", the shell is standing in the wrong folder, or the name is misspelled. Print the current directory (`cd` on Windows with no argument, `pwd` on Mac/Linux). Use Tab to complete names instead of guessing.\n\nThe live panel already stands in the lesson folder, so the filename is enough. On your laptop you have to `cd` there first, or pass a path.\n\n## 5. Arguments (the rest of the command)\n\nAnything after the script name is an argument. Python exposes the whole line as `sys.argv`: item 0 is the script name, item 1 is the first extra word, and so on.\n\n**Try it here:** button **3. Pass a name**. That runs `demoPrintArgScript.py` with `Ada` after it. Read the output: you should see `Ada` in the argument list.\n\nTyping positions by hand gets old. `argparse` lets you name them:\n\n```text\npython demoArgParseScript.py -h\npython demoArgParseScript.py --name Ryan\n```\n\n**Try it here:** button **4. Named flag**. Same idea as `grok --cwd ...`: a flag, then a value.\n\nIf a program prints a help screen and quits, you probably passed `-h` or forgot a required flag. Read the help; it is the contract.\n\n## 6. Standard input and pipes\n\nSome programs read a stream of text instead of (or in addition to) arguments. That stream is standard input. In a terminal you can type it, or you can pipe another command's output in:\n\n```text\necho Hello World | python demoStdinScript.py\n```\n\nOn PowerShell, `echo` still works for a short line. The script here uppercases each non-empty line.\n\n**Try it here:** button **5. Pipe text in**.\n\nArguments are the ticket notes. Standard input is the bag of ingredients. Use arguments for short, named settings. Use a pipe or a redirected file when the data is longer.\n\nThe box at the bottom of the live panel is a scratch pad: a few lines of Python that are not saved as a file. Use it to try `print` and arithmetic. It is not a replacement for a `.py` file you intend to keep.\n\n## 7. Virtual environments and packages\n\n`pip install` into the interpreter you use for everything will eventually break something. A virtual environment is a small, project-local Python: same language, its own package list.\n\n```text\npython -m venv .venv\n```\n\nActivate it before you install:\n\n```text\n.venv\\Scripts\\activate\n```\n\nOn Mac and Linux: `source .venv/bin/activate`.\n\nThen:\n\n```text\npip install numpy\npip install -r requirements.txt\n```\n\n`requirements.txt` in this lesson is a pinned list for later demos. You do not need it for the buttons on the right. When you work on your own laptop, create the venv in the project folder, activate it, and install from the file. The prompt usually shows `(.venv)` when it is on.\n\nNever commit a `venv` folder into a Google Drive tree. Keep environments outside shared vaults.\n\n## 8. Shell scripts\n\nIf you can type a command, you can save it.\n\nOn Windows, `start.bat` in this lab is a two-line example: change to the lab folder, then start the server. On Mac and Linux you would write a `.sh` file and `chmod +x` it.\n\nA shell script can call Python, call `grok`, copy files, or chain pipes. It is still just lines the shell would have accepted one at a time. Put flags you always want (cwd, a log path) in the script so you do not forget them.\n\nWhen a script fails, the error is usually from the line it was running, not from \"batch files\" as a concept. Comment lines (`rem` / `::` on Windows, `#` on Unix) so a future you can see why the flags are there.\n\n## Closing\n\nYou should be able to: find Python, stand in a folder, run a `.py` file, pass arguments, pipe text, keep packages in a venv, and recognize that `grok` and a `.bat`/`.sh` file are the same kind of object as `python` \u2014 a program plus a line.\n\nIf a later session asks you to run something and it \"does nothing,\" check three things in order: is the program on PATH, are you in the right folder, and did you pass the arguments that tool actually reads.\n";
